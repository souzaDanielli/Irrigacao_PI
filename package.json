{
  "dependencies": {
    "@prisma/client": "^6.8.1",
    "axios": "^1.9.0",
    "express": "^4.21.2",
    "pg": "^8.14.1"
  },
  "scripts": {
    "start": "node server.js",
    "build": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy"
  },
  "devDependencies": {
    "prisma": "^6.8.1"
  }
}
